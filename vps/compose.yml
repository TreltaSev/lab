services:
  caddy:
    container_name: cps-caddy
    build: .
    ports:
      - "25565:25565"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config: